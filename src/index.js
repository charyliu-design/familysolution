import React from 'react';
import ReactDOM from 'react-dom/client';
import KidsActivityScheduler from './KidsActivityScheduler';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <KidsActivityScheduler />
  </React.StrictMode>
);